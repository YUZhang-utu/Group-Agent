# E049 survivor validation protocol

Rebuild the E048 spread-plus-boundary panel under the sealed selection policy.
Save retained IDs. Score every coarse survivor plus up to 64 spread rejected IDs
with the reference and optimized paths. Compare membership and all surviving
arrays exactly. Report individual and cumulative anchor failures, without changing
thresholds. Crystal-query self matching at identity and at the Gaussian-selected
pose is a synthetic implementation control, not an independent library positive.
No automatic full-library job. A zero-positive library panel remains inconclusive
for positive retention. Local fixtures precede workstation execution.

User-authorized extension: run the uncapped full-library funnel after the small
correctness audit, verify coverage, enumerate all matching molecule representatives,
and reference-reproduce up to 128 final matching molecules. Persist stages and
resume only verified full-library chunks under unchanged protocol/code/inputs.
No-hit equivalent panels permit the requested scan but do not validate positives.
