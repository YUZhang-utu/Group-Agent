# E055: explicit score contracts and evidence audit

## Protocol before execution

Hypothesis: a frozen optional-contact budget makes the contribution of an unchanged
contact invariant to deletion or reweighting of other contacts. Legacy weighted
means must remain reproducible. Test analytical scores, family maximum semantics,
budget overflow rejection, serialization through adoption, and independent spatial
group ambiguity margins (including group-order invariance).

Implement explicit backbone/sidechain and ligand-role annotations without changing
anchor IDs. Audit multi-template union semantics and report the absence of
cross-template statistical calibration. Inspect the supplied LYS94 mode from the
sealed local consensus and contact ledger; do not infer affinity or solubility.

Run focused regressions, then the full local suite and English guard. Prepare a
portable, read-only score audit accepting a consensus recommendation report and a
new output directory. Run it on local real structures with an explicitly labelled
fixture recommendation. Report per-template crystal-pose score distributions and
source observations, not independent active recall. No library scan or production
design adoption is authorized by this protocol. The user's later workstation
validation must use fresh outputs and preserve source hashes.

The 0.4/0.4/0.2 mix and per-region variance estimation remain exploratory proposals;
do not enable them without reviewed regions and independent validation data.
